#input() prompt user to input a name from the console
#save the name from the user in the variable 'name'
#used the len() to print the lengh of the name.
name = input("What is your name? ")
print(len(name))